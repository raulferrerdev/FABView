import XCTest
@testable import FABView

final class FABViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FABView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
